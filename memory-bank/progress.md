# Progress

- Implemented `eval()` and `concat()` built-in macros in `tools/parser2.py`.
- Refactored macro expansion logic in `tools/parser2.py` to perform true token replacement, fixing the accumulation issue.
